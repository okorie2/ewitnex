/** @jsxImportSource @emotion/react */

import React from "react";
import { theme } from "styles/theme";

const DashboardHeader = () => {
  return (
    <div
      css={{ height: "80px", borderBottom: `1px solid ${theme.shadow.border}` }}
    >
      DashboardHeader
    </div>
  );
};
export default DashboardHeader;
